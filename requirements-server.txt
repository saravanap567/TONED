# Slim requirements for Docker deployment (server + inference only)
# Excludes gradio, matplotlib, pillow to reduce image size and memory

# Core
numpy>=1.24.0
pyyaml>=6.0
pydantic>=2.0.0

# OpenAI SDK (required by hackathon)
openai>=1.0.0

# FastAPI server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Async HTTP client (inference agent)
httpx>=0.27.0
